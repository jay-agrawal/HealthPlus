import 'package:flutter/material.dart';

class Services extends StatelessWidget {
  static String routeName = "/services-page";
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(
        child: Text('Services Page'),
      ),
    );
  }
}
